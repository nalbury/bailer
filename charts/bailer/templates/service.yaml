apiVersion: v1
kind: Service
metadata:
  name: {{ template "bailer.fullname" . }}
  labels:
{{ include "bailer.labels" . | indent 4 }}
spec:
  type: {{ .Values.bailer.service.type }}
  ports:
    - port: {{ .Values.bailer.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app.kubernetes.io/name: {{ include "bailer.name" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
